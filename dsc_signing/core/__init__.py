"""Pure cryptographic core for DSC deferred PAdES signing.

These modules are intentionally free of any web-framework / database imports so
they can be unit-tested (and reused) in complete isolation.
"""
